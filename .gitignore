# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment — .env é commitado porque contém APENAS chaves anon/publishable
# do Supabase (protegidas por RLS, já visíveis no bundle do frontend).
# .env.local e .env.*.local NUNCA entram no repo (são pra secrets de verdade, se houver)
.env.local
.env.*.local

# Claude Code — personal overrides stay local
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
