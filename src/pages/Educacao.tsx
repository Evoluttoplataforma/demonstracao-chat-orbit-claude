import { useState, useEffect, useMemo, useRef } from "react";
import { validateEmail } from "@/lib/email-validation";
import { pushFormSubmitSuccess } from "@/lib/dataLayer";
import { useNavigate } from "react-router-dom";
import { supabase } from "@/integrations/supabase/client";
import PhoneInputWithCountry from "@/components/PhoneInputWithCountry";
import { getEducacaoSessionVariant } from "@/components/chat/educacaoCopyVariants";
import oliviaImg from "@/assets/olivia-real-optimized.jpg";
import orbitLogoColor from "@/assets/orbit-logo-color.png";
import {
  CheckCircle, ChevronDown, Star, Shield,
  Search, FolderCog, Rocket, ClipboardCheck, Quote,
  X, User, Mail, Phone, Building2, MessageSquare,
  BarChart3, Users, Zap, Brain, Target, Briefcase,
  GraduationCap, FileText, TrendingUp, Calendar,
  Instagram, MapPin, BookOpen,
} from "lucide-react";

const faqItems = [
  { q: "O que é o Orbit?", a: "O Orbit é uma plataforma que entrega dezenas de agentes de IA especializados para executar a gestão da sua escola ou instituição de ensino. Cada agente cuida de uma área — processos, pessoas, indicadores, treinamento e mais. Eles trabalham 24/7, integrados aos dados do seu negócio." },
  { q: "Funciona para escolas pequenas?", a: "Sim. Funciona para instituições de 3 a 200 pessoas. Na verdade, quanto menor a equipe, mais impacto os agentes geram — porque substituem funções que você não consegue contratar." },
  { q: "Quanto tempo leva para implementar?", a: "Em até 7 dias seu time de agentes está operando. A Olívia, nossa especialista em IA, analisa o contexto da sua escola e configura os agentes ideais para sua operação." },
  { q: "Preciso parar minha operação?", a: "Não. Os agentes se integram à sua operação atual. Nada para, nada muda de repente. Eles começam atuando em paralelo e vão assumindo gradualmente." },
  { q: "A demonstração é gratuita?", a: "Sim. São 30 minutos para você conhecer seus agentes, entender como atuariam na sua escola e ver resultados reais. Sem compromisso." },
  { q: "E se minha escola já usa ferramentas de gestão?", a: "Perfeito. O Orbit não substitui suas ferramentas — ele preenche a lacuna entre ter a ferramenta e realmente usar. Os agentes garantem que processos sejam seguidos, indicadores monitorados e equipe treinada." },
];

const painPoints = [
  { bold: "Cada decisão depende de mim.", sub: "Se eu não estou, a escola trava." },
  { bold: "Perco alunos por falta de organização,", sub: "não por falta de qualidade pedagógica." },
  { bold: "Minha equipe vive apagando incêndio", sub: "e eu fico preso no operacional." },
  { bold: "Inadimplência crescendo", sub: "e nenhum processo para conter." },
  { bold: "Não consigo tirar férias", sub: "porque nada funciona sem mim." },
  { bold: "Já tentei planilha, sistema, consultor…", sub: "e nada se mantém funcionando." },
];

const agents = [
  { icon: Target, name: "Ag. Estrategista", desc: "Constrói planejamento estratégico, análise de mercado, objetivos e planos de ação para a escola" },
  { icon: FolderCog, name: "Ag. de Processos", desc: "Mapeia processos administrativos e pedagógicos, gera instruções de trabalho e playbooks" },
  { icon: Users, name: "Ag. de Pessoas", desc: "Descreve cargos, analisa desempenho, cria PDIs e acompanha evolução da equipe" },
  { icon: GraduationCap, name: "Ag. de Treinamento", desc: "Cria microlearning diário sobre práticas pedagógicas, atendimento e gestão via WhatsApp" },
  { icon: BarChart3, name: "Ag. de Indicadores", desc: "Monitora KPIs da escola (matrículas, evasão, NPS), sugere ações corretivas" },
  { icon: Search, name: "Ag. de Pesquisa", desc: "Analisa pesquisas de satisfação de pais e alunos, clima organizacional e gera insights" },
  { icon: BookOpen, name: "Ag. Pedagógico", desc: "Monitora qualidade de ensino, engajamento de alunos e propõe melhorias curriculares" },
  { icon: Briefcase, name: "Ag. de Oportunidades", desc: "Mapeia oportunidades de novos cursos, turmas extras e expansão de receita" },
  { icon: ClipboardCheck, name: "Ag. de Problemas", desc: "Registra não conformidades, causa raiz, planos de ação corretivos" },
  { icon: FileText, name: "Ag. de Documentos", desc: "Gera documentos corporativos estruturados conectados aos processos da escola" },
  { icon: TrendingUp, name: "Ag. de Matrículas", desc: "Estrutura funil de captação de alunos, cadências de follow-up e análise de conversão" },
  { icon: Calendar, name: "Ag. de Reuniões", desc: "Gera pautas, registra decisões, distribui ações pós-reunião" },
];

const results = [
  { value: "-40%", label: "Retrabalho", desc: "Agentes de Processos e Documentos reduziram 40% do tempo em refação" },
  { value: "+65%", label: "Produtividade", desc: "Equipes com agentes de Treinamento e Indicadores pararam de 'bater cabeça'" },
  { value: "-30%", label: "Evasão", desc: "Agente Pedagógico e de Pesquisa identificam riscos antes da desistência" },
  { value: "7 dias", label: "Implementação", desc: "Do onboarding ao primeiro resultado em uma semana" },
];

const processSteps = [
  { icon: Zap, number: "01", title: "Conecte sua escola", desc: "Cadastre sua escola em menos de 5 minutos. A Olívia analisa o contexto do seu negócio." },
  { icon: Brain, number: "02", title: "Seus agentes são configurados", desc: "Com base na análise, a Olívia ativa os agentes ideais para sua operação educacional. Cada um recebe o contexto da sua escola." },
  { icon: Rocket, number: "03", title: "Seu time de IA começa a executar", desc: "Os agentes iniciam a operação: mapeiam processos, criam indicadores, treinam equipe e monitoram resultados — automaticamente." },
];

const testimonials = [
  { name: "Rogério Menossi", company: "CEO — Time Produtivo", text: "Em dois meses usando Orbit aumentamos em 25% nossa receita recorrente.", initial: "R" },
  { name: "Pedro Muchitz", company: "Weagle", text: "Com o Orbit estamos conseguindo padronizar a gestão em unidades no Brasil, EUA e Japão.", initial: "P" },
];

const FormInput = ({ label, icon: Icon, name, value, onChange, type = "text", autoComplete }: { label: string; icon: any; name: string; value: string; onChange: (e: React.ChangeEvent<HTMLInputElement>) => void; type?: string; autoComplete?: string; }) => {
  const [focused, setFocused] = useState(false); const hasValue = value.length > 0; const inputName = autoComplete === "off" ? `${name}_${Math.random().toString(36).slice(2, 6)}` : name;
  return (<div className={`relative rounded-2xl transition-all duration-300 bg-card border-2 ${focused ? "border-primary shadow-lg shadow-primary/10 scale-[1.02]" : "border-border hover:border-primary/40"}`}><label className={`absolute left-4 transition-all duration-200 pointer-events-none z-10 ${focused || hasValue ? "top-2 text-xs font-medium text-primary" : "top-1/2 -translate-y-1/2 text-sm text-muted-foreground"}`}>{label}</label><div className="flex items-center"><input type={type} name={inputName} value={value} onChange={(e) => { const se = { ...e, target: { ...e.target, name, value: e.target.value } } as React.ChangeEvent<HTMLInputElement>; onChange(se); }} autoComplete={autoComplete || "off"} autoCorrect="off" onFocus={() => setFocused(true)} onBlur={() => setFocused(false)} className="w-full px-4 pt-6 pb-3 bg-transparent outline-none text-foreground text-base font-medium pr-12 placeholder:text-transparent" /><div className={`absolute right-4 transition-all duration-300 ${focused ? "text-primary scale-110" : "text-muted-foreground"}`}><Icon className="w-5 h-5" /></div></div></div>);
};

const FAQSection = () => { const [openIndex, setOpenIndex] = useState<number | null>(null); return (<div className="space-y-2">{faqItems.map((item, i) => (<div key={i} className="rounded-xl border border-border bg-card/50 overflow-hidden"><button onClick={() => setOpenIndex(openIndex === i ? null : i)} className="w-full flex items-center justify-between gap-3 p-4 text-left"><span className="text-sm font-medium text-foreground/90">{item.q}</span><ChevronDown className={`w-4 h-4 text-muted-foreground shrink-0 transition-transform duration-200 ${openIndex === i ? "rotate-180" : ""}`} /></button>{openIndex === i && (<div className="px-4 pb-4 text-sm text-muted-foreground leading-relaxed animate-fade-in">{item.a}</div>)}</div>))}</div>); };

const NICHE_LABEL = "Educação";
const NICHE_CARGO = "Dono / Sócio de Instituição de Ensino";
const NICHE_SETOR = "Educação";
const NICHE_ICON = BookOpen;
const NICHE_TAG = "Orbit para Educação — Gestão com IA";
const NICHE_ENTITY = "escola";
const NICHE_ENTITY_FULL = "instituição de ensino";

const Educacao = () => {
  const navigate = useNavigate(); const pageStartRef = useRef(Date.now());
  const [showModal, setShowModal] = useState(false); const [showStickyBar, setShowStickyBar] = useState(false);
  const [formData, setFormData] = useState({ name: "", email: "", phone: "", company: "" });
  const [phoneFullNumber, setPhoneFullNumber] = useState("+55"); const [consentido, setConsentido] = useState(false);
  const variant = useMemo(() => getEducacaoSessionVariant(), []);
  const isValid = formData.name && formData.email && formData.phone && formData.company && consentido;
  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => { const { name, value } = e.target; setFormData((prev) => ({ ...prev, [name]: value })); };
  const [submitting, setSubmitting] = useState(false); const [emailError, setEmailError] = useState("");

  const handleSubmit = async () => {
    if (!isValid || submitting) return;
    const emailCheck = validateEmail(formData.email.trim());
    if (!emailCheck.valid) { setEmailError(emailCheck.error || "E-mail inválido"); return; }
    setEmailError(""); setSubmitting(true);
    const parts = formData.name.trim().split(" "); const nome = parts[0]; const sobrenome = parts.slice(1).join(" "); const copyVariant = variant.id;
    const params = new URLSearchParams(window.location.search);
    const utmFields = { utm_source: params.get('utm_source') || undefined, utm_medium: params.get('utm_medium') || undefined, utm_campaign: params.get('utm_campaign') || undefined, utm_content: params.get('utm_content') || undefined, utm_term: params.get('utm_term') || undefined, gclid: params.get('gclid') || undefined, fbclid: params.get('fbclid') || undefined, gbraid: params.get('gbraid') || undefined, wbraid: params.get('wbraid') || undefined, ttclid: params.get('ttclid') || undefined, gad_campaignid: params.get('gad_campaignid') || undefined, gad_source: params.get('gad_source') || undefined, msclkid: params.get('msclkid') || undefined, li_fat_id: params.get('li_fat_id') || undefined, sck: params.get('sck') || undefined, landing_page: window.location.href, origin_page: window.location.pathname, session_attributes_encoded: params.get('session_attributes_encoded') || undefined, apex_session_id: sessionStorage.getItem('apex_session_id') || undefined };
    const cleanUtm = Object.fromEntries(Object.entries(utmFields).filter(([, v]) => v !== undefined));
    try {
      const phoneDigitsForLookup = formData.phone.replace(/\D/g, '');
      const { data: existingLead } = await supabase.from("leads").select("id").or(`email.eq.${formData.email},whatsapp.ilike.%${phoneDigitsForLookup}%`).order("created_at", { ascending: false }).limit(1).maybeSingle();
      let leadId: string | null = null;
      const leadPayload = { nome, sobrenome, whatsapp: phoneFullNumber, email: formData.email, empresa: formData.company, status: "parcial", copy_variant: copyVariant, cargo: NICHE_CARGO, oque_faz: NICHE_LABEL, ...cleanUtm } as any;
      if (existingLead) { await supabase.from("leads").update(leadPayload).eq("id", existingLead.id); leadId = existingLead.id; } else { const { data: inserted } = await supabase.from("leads").insert(leadPayload).select("id").single(); leadId = inserted?.id || null; }
      supabase.functions.invoke('sync-lead-crm', { body: { nome: formData.name, email: formData.email, whatsapp: formData.phone, empresa: formData.company || '' } });
      try { const phoneDigits = formData.phone.replace(/\D/g, ''); let sid = sessionStorage.getItem('apex_session_id'); if (!sid) { sid = Date.now() + '_' + Math.random().toString(36).substr(2, 9); sessionStorage.setItem('apex_session_id', sid); } pushFormSubmitSuccess({ email: formData.email, phoneNumber: phoneDigits.replace(/^55/, ''), nome, sobrenome, apex_session_id: sid, time_on_page_at_submit: Math.round((Date.now() - pageStartRef.current) / 1000) }); } catch (e) { console.error('dataLayer push error:', e); }
      // Fire-and-forget: Pipedrive creation runs in background
      supabase.functions.invoke('create-pipedrive-lead', { body: { action: 'create', name: formData.name.trim(), whatsapp: phoneFullNumber, email: formData.email, empresa: formData.company, cargo: NICHE_CARGO, oqueFaz: NICHE_LABEL, leadId, copyVariant, utmData: cleanUtm } }).then(({ data: pipeResult, error: pipeError }) => { if (!pipeError && pipeResult?.success) { console.log("LP Educação: Pipedrive lead criado (background):", pipeResult.deal_id); supabase.functions.invoke("create-pipedrive-lead", { body: { action: "add_label", deal_id: pipeResult.deal_id, label_name: "CHAT1", label_color: "blue" } }).catch(() => {}); } }).catch((e) => console.error("LP Educação: Pipedrive exception (background):", e));
      sessionStorage.setItem("orbit_lp_data", JSON.stringify({ ...formData, nome, sobrenome, copyVariant, leadId, pipedriveIds: {}, utmData: cleanUtm, oqueFaz: NICHE_LABEL, cargo: NICHE_CARGO, nicheSetor: NICHE_SETOR }));
      navigate("/chat");
    } catch (err) { console.error("Failed to save lead:", err); sessionStorage.setItem("orbit_lp_data", JSON.stringify({ ...formData, copyVariant })); navigate("/chat"); } finally { setSubmitting(false); }
  };

  useEffect(() => { const container = document.getElementById("lp-scroll"); if (!container) return; const handler = () => setShowStickyBar(container.scrollTop > 400); container.addEventListener("scroll", handler, { passive: true }); return () => container.removeEventListener("scroll", handler); }, []);

  return (
    <div id="lp-scroll" className="h-dvh flex flex-col bg-background text-foreground overflow-y-auto animate-fade-in">
      <div className={`fixed top-0 left-0 right-0 z-40 transition-all duration-300 ${showStickyBar ? "translate-y-0 opacity-100" : "-translate-y-full opacity-0"}`}><div className="bg-background/95 backdrop-blur-md border-b border-border px-4 py-2.5"><div className="max-w-6xl mx-auto flex items-center justify-between gap-4"><div className="flex items-center gap-2"><img src={orbitLogoColor} alt="Orbit" className="h-7 w-auto" /><span className="font-bold text-foreground text-sm">Orbit</span></div><button onClick={() => setShowModal(true)} className="bg-primary hover:bg-primary/90 text-primary-foreground font-bold text-xs sm:text-sm px-5 py-2 rounded-lg transition-colors whitespace-nowrap">{variant.cta}</button></div></div></div>

      {showModal && (<div className="fixed inset-0 z-50 flex items-center justify-center px-4 animate-fade-in"><div className="absolute inset-0 bg-black/70 backdrop-blur-sm" onClick={() => setShowModal(false)} /><div className="relative bg-background border border-border rounded-2xl w-full max-w-md p-6 shadow-2xl"><button onClick={() => setShowModal(false)} className="absolute top-4 right-4 w-8 h-8 rounded-full bg-secondary flex items-center justify-center hover:bg-secondary/80 transition-colors"><X className="w-4 h-4 text-foreground" /></button><h3 className="text-lg font-bold text-primary text-center mb-1">Preencha para iniciar:</h3><p className="text-sm text-muted-foreground text-center mb-6">100% gratuito • Sem compromisso</p><div id="lead-form" className="space-y-4 mb-6"><FormInput name="name" value={formData.name} onChange={handleChange} label="Nome Completo *" icon={User} autoComplete="name" /><div><FormInput type="email" name="email" value={formData.email} onChange={(e) => { handleChange(e); if (emailError) setEmailError(""); }} label="Email *" icon={Mail} autoComplete="email" />{emailError && <p className="text-destructive text-sm mt-1 px-1">{emailError}</p>}</div><PhoneInputWithCountry variant="card" onValueChange={(full, raw) => { setPhoneFullNumber(full); setFormData((prev) => ({ ...prev, phone: raw })); }} /><FormInput name="company" value={formData.company} onChange={handleChange} label="Nome da Escola *" icon={Building2} /></div><label className="flex items-start gap-3 cursor-pointer group mb-4"><div className="pt-0.5"><div onClick={() => setConsentido(!consentido)} className={`w-5 h-5 rounded border-2 flex items-center justify-center transition-all duration-200 ${consentido ? "bg-primary border-primary" : "border-muted-foreground/40 group-hover:border-primary/60"}`}>{consentido && (<svg className="w-3 h-3 text-primary-foreground" viewBox="0 0 12 12" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M2 6l3 3 5-5" /></svg>)}</div></div><span className="text-xs text-muted-foreground leading-relaxed">Ao preencher este formulário, concordo em compartilhar meus dados com a Orbit para fins de contato e demonstração, conforme a{" "}<a href="/privacidade" target="_blank" className="text-primary underline hover:text-primary/80">Política de Privacidade</a>.</span></label><button onClick={handleSubmit} disabled={!isValid || submitting} className="w-full py-4 px-8 rounded-xl font-extrabold text-lg tracking-wide uppercase transition-all duration-200 flex items-center justify-center gap-2 bg-primary text-primary-foreground disabled:opacity-50 disabled:cursor-not-allowed hover:brightness-110 active:scale-[0.98] shadow-lg shadow-primary/30"><MessageSquare className="w-5 h-5" />{submitting ? "ENVIANDO..." : "INICIAR CONVERSA"}</button></div></div>)}

      <section className="relative px-5 pt-6 pb-6 flex flex-col items-start text-left lg:px-16 lg:pt-20 lg:pb-16"><div className="w-full max-w-6xl mx-auto"><div className="flex items-center gap-2 mb-5"><img src={orbitLogoColor} alt="Orbit" className="h-10 w-auto" /><span className="font-bold text-foreground text-2xl">Orbit</span></div><div className="absolute inset-0 overflow-hidden pointer-events-none"><div className="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] rounded-full bg-primary/15 blur-3xl" /></div><div className="lg:grid lg:grid-cols-2 lg:gap-12 lg:items-center"><div><span className="inline-flex items-center gap-1.5 bg-primary/15 text-primary px-4 py-1.5 rounded-full text-xs font-bold mb-5 border border-primary/25 relative z-10 uppercase tracking-wider"><NICHE_ICON className="w-3.5 h-3.5 shrink-0" />{NICHE_TAG}</span><h1 className="text-[1.75rem] sm:text-4xl lg:text-[2.75rem] lg:leading-[1.12] font-extrabold leading-[1.15] mb-4 relative z-10 uppercase tracking-tight">{variant.headline}{" "}<span className="text-primary">{variant.highlightedPart}</span></h1><p className="text-[0.95rem] lg:text-lg text-foreground/80 mb-6 relative z-10 leading-relaxed">{variant.description}</p><div className="w-full max-w-sm relative z-10 mb-3"><button onClick={() => setShowModal(true)} className="w-full py-5 px-8 rounded-xl font-extrabold text-lg tracking-wide uppercase transition-all duration-200 flex items-center justify-center gap-2 bg-primary text-primary-foreground hover:brightness-110 active:scale-[0.98] shadow-lg shadow-primary/30">{variant.cta}</button><p className="text-center text-xs text-muted-foreground mt-2.5">⏱️ 2 min • 100% gratuito • Sem compromisso</p></div><ChevronDown className="w-5 h-5 text-muted-foreground/40 animate-bounce mt-2 hidden lg:block" /></div><div className="hidden lg:flex flex-col items-center justify-center gap-6 relative z-10"><div className="w-full max-w-md space-y-4"><div className="bg-card/50 border border-border rounded-2xl p-6 backdrop-blur-sm"><span className="inline-flex items-center gap-1.5 bg-primary/15 text-primary px-3 py-1 rounded-full text-xs font-bold border border-primary/25 uppercase tracking-wider mb-4"><BarChart3 className="w-3.5 h-3.5 shrink-0" />O que os agentes resolvem na sua escola</span><div className="space-y-3 mt-2">{[{ problem: "Gestão caótica", solved: "Processos padronizados" },{ problem: "Equipe sem direção", solved: "Treinamento contínuo" },{ problem: "Evasão crescente", solved: "Monitoramento preditivo" },{ problem: "Inadimplência alta", solved: "Indicadores monitorados" }].map((item) => (<div key={item.problem} className="flex items-center gap-3"><div className="flex-1"><div className="flex items-center justify-between text-xs mb-1"><span className="text-muted-foreground line-through">{item.problem}</span><span className="text-primary font-bold">{item.solved}</span></div><div className="h-1.5 rounded-full bg-secondary overflow-hidden"><div className="h-full rounded-full bg-gradient-to-r from-primary to-primary/60" style={{ width: "100%" }} /></div></div></div>))}</div></div><div className="grid grid-cols-3 gap-3">{[{ val: "Dezenas", label: "Agentes de IA" },{ val: "2.206+", label: "Empresas" },{ val: "4,9★", label: "Google" }].map((s) => (<div key={s.label} className="bg-card/50 border border-border rounded-xl p-3 text-center"><div className="text-xl font-extrabold text-primary">{s.val}</div><div className="text-[10px] text-muted-foreground uppercase tracking-wider mt-0.5">{s.label}</div></div>))}</div></div></div></div><ChevronDown className="w-5 h-5 text-muted-foreground/40 animate-bounce mt-2 lg:hidden" /></div></section>

      <section className="px-5 py-8 lg:py-12 lg:px-16"><div className="max-w-2xl mx-auto"><div className="rounded-[20px] border border-primary/30 bg-card p-6 lg:p-8"><div className="flex items-start gap-4"><img src={oliviaImg} loading="lazy" alt="Olívia" className="w-16 h-16 rounded-full object-cover object-[center_20%] shrink-0 border-2 border-primary" /><div><p className="font-bold text-primary text-lg">Olívia</p><p className="text-sm text-muted-foreground">Especialista em IA do Orbit</p></div></div><p className="mt-5 text-foreground/90 italic text-base lg:text-lg leading-relaxed">"Eu coordeno dezenas de agentes de IA dentro do Orbit. Quando sua {NICHE_ENTITY} se conecta, eu analiso o contexto do seu negócio e configuro os agentes ideais para sua operação. Eles estruturam processos, treinam equipe, monitoram KPIs e reduzem evasão — sem depender só de você."</p></div><p className="text-center text-sm text-muted-foreground mt-6">Veja se você se reconhece nos desafios abaixo:</p><div className="flex justify-center mt-3"><ChevronDown className="w-5 h-5 text-primary animate-bounce" /></div></div></section>

      <section className="bg-secondary/50"><div className="max-w-6xl mx-auto px-5 py-8 lg:py-14 lg:px-16"><div className="text-center mb-8"><h2 className="text-xl lg:text-3xl font-extrabold text-foreground uppercase tracking-tight mb-2">O caos invisível de toda <span className="text-primary">{NICHE_ENTITY_FULL}:</span></h2><p className="text-sm lg:text-base text-muted-foreground max-w-2xl mx-auto">Você abriu uma escola para transformar vidas, mas acabou preso em um ciclo infinito de matrículas, inadimplência, retrabalho e cobrança da equipe.</p></div><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 max-w-4xl mx-auto">{painPoints.map((item, i) => (<div key={i} className="flex items-start gap-3 p-4 rounded-xl bg-card/50 border border-border/50 hover:border-primary/30 transition-all duration-300"><div className="w-1.5 h-1.5 rounded-full bg-primary mt-2 flex-shrink-0" /><div><p className="text-sm font-semibold text-foreground leading-snug">{item.bold}</p><p className="text-xs text-muted-foreground mt-0.5">{item.sub}</p></div></div>))}</div><div className="text-center mt-8"><button onClick={() => setShowModal(true)} className="bg-primary hover:bg-primary/90 text-primary-foreground font-bold text-sm px-8 py-3 rounded-xl transition-colors uppercase tracking-wide shadow-lg shadow-primary/20">QUERO SAIR DESSE CAOS →</button></div></div></section>

      <section className="px-5 py-8 lg:py-14 lg:px-16"><div className="max-w-6xl mx-auto"><div className="text-center mb-8 lg:mb-12"><h2 className="text-xl lg:text-3xl font-extrabold text-foreground uppercase tracking-tight mb-2">Conheça o time de IA que vai{" "}<span className="text-primary">organizar sua {NICHE_ENTITY}</span></h2><p className="text-sm text-muted-foreground max-w-2xl mx-auto">Cada agente é especialista em uma área da gestão. Eles trabalham 24/7 dentro da sua operação — sem férias, sem retrabalho, sem desculpas.</p></div><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3 lg:gap-4">{agents.map((a, i) => (<div key={i} className="bg-card border border-border rounded-xl p-4 hover:border-primary/30 hover:shadow-lg hover:shadow-primary/5 transition-all duration-300 group"><div className="flex items-center gap-2 mb-2"><div className="w-8 h-8 rounded-lg bg-primary/10 flex items-center justify-center group-hover:scale-110 transition-transform"><a.icon className="w-4 h-4 text-primary" /></div><h3 className="text-xs font-bold text-primary">{a.name}</h3></div><p className="text-xs text-foreground/80 leading-relaxed">{a.desc}</p></div>))}</div><div className="mt-8 rounded-[20px] border border-primary/30 bg-card p-6 lg:p-8 text-center"><p className="text-base lg:text-lg text-foreground/90 leading-relaxed">Se você contratasse esse time como CLT, gastaria entre{" "}<span className="text-destructive line-through font-bold">R$100.000 e R$200.000/mês</span>.{" "}Com o Orbit, tudo isso custa a partir de{" "}<span className="text-success font-extrabold text-xl lg:text-2xl">R$1.200/mês</span>.</p></div></div></section>

      <section className="px-5 py-8 lg:py-14 lg:px-16 bg-secondary/50"><div className="max-w-6xl mx-auto"><div className="text-center mb-8 lg:mb-12"><h2 className="text-xl lg:text-3xl font-extrabold text-foreground uppercase tracking-tight mb-2">Resultados reais de quem{" "}<span className="text-primary">saiu do caos</span></h2></div><div className="grid grid-cols-2 lg:grid-cols-4 gap-3 lg:gap-4">{results.map((r, i) => (<div key={i} className="bg-card border border-border rounded-xl p-5 text-center hover:border-primary/30 transition-all duration-300"><div className="text-3xl lg:text-4xl font-extrabold text-primary mb-1">{r.value}</div><p className="text-sm font-semibold text-foreground mb-2">{r.label}</p><p className="text-xs text-muted-foreground leading-relaxed">{r.desc}</p></div>))}</div></div></section>

      <section className="px-5 py-8 lg:py-14 lg:px-16"><div className="max-w-6xl mx-auto"><div className="text-center mb-8 lg:mb-12"><h2 className="text-xl lg:text-3xl font-extrabold text-foreground uppercase tracking-tight mb-2">Como funciona?{" "}<span className="text-primary">3 passos para começar</span></h2></div><div className="hidden lg:block relative"><div className="absolute top-[80px] left-[15%] right-[15%] h-0.5 bg-gradient-to-r from-primary/50 via-primary/30 to-primary/50 rounded-full" /><div className="grid grid-cols-3 gap-8 max-w-3xl mx-auto">{processSteps.map((step, i) => (<div key={i} className="relative group"><div className="bg-card rounded-xl p-6 border border-border hover:border-primary/30 hover:shadow-lg hover:shadow-primary/5 transition-all duration-300 text-center hover:-translate-y-1"><div className="w-14 h-14 mx-auto rounded-xl bg-primary/10 flex items-center justify-center mb-3 group-hover:scale-110 transition-transform duration-300 relative"><step.icon className="w-7 h-7 text-primary" /><span className="absolute -top-2 -right-2 w-6 h-6 rounded-full bg-primary text-primary-foreground text-xs font-bold flex items-center justify-center shadow-lg">{i + 1}</span></div><h3 className="text-base font-bold text-foreground mb-1">{step.title}</h3><p className="text-sm text-muted-foreground">{step.desc}</p></div></div>))}</div></div><div className="lg:hidden max-w-sm mx-auto"><div className="relative"><div className="absolute left-5 top-0 bottom-0 w-0.5 bg-gradient-to-b from-primary/50 via-primary/30 to-primary/50 rounded-full" /><div className="space-y-4">{processSteps.map((step, i) => (<div key={i} className="relative flex gap-3"><div className="flex-shrink-0 w-10 h-10 rounded-xl bg-primary/10 border border-primary/30 flex items-center justify-center z-10 relative"><step.icon className="w-5 h-5 text-primary" /><span className="absolute -top-1.5 -right-1.5 w-5 h-5 rounded-full bg-primary text-primary-foreground text-[10px] font-bold flex items-center justify-center">{i + 1}</span></div><div className="flex-1 bg-card rounded-xl p-3 border border-border"><span className="text-[10px] font-bold text-primary bg-primary/10 px-2 py-0.5 rounded-full uppercase">Passo {step.number}</span><h3 className="text-base font-bold text-foreground mt-1.5 mb-0.5">{step.title}</h3><p className="text-xs text-muted-foreground">{step.desc}</p></div></div>))}</div></div></div></div></section>

      <section className="px-5 py-8 lg:py-14 lg:px-16 bg-secondary/50"><div className="max-w-sm lg:max-w-3xl mx-auto"><div className="grid grid-cols-3 gap-3 lg:gap-8 text-center mb-5 lg:mb-10"><div><div className="text-2xl lg:text-4xl font-extrabold text-primary">Dezenas</div><div className="text-xs lg:text-sm text-muted-foreground uppercase tracking-wider mt-1">Agentes de IA</div></div><div><div className="text-2xl lg:text-4xl font-extrabold text-primary">2.206+</div><div className="text-xs lg:text-sm text-muted-foreground uppercase tracking-wider mt-1">Empresas atendidas</div></div><div><div className="text-2xl lg:text-4xl font-extrabold text-primary flex items-center justify-center gap-0.5">4,9 <Star className="w-4 h-4 lg:w-6 lg:h-6 fill-primary" /></div><div className="text-xs lg:text-sm text-muted-foreground uppercase tracking-wider mt-1">Google</div></div></div><div className="flex items-center gap-3 p-3 lg:p-5 rounded-xl bg-primary/10 border border-primary/25"><div className="w-10 h-10 rounded-full bg-primary/20 flex items-center justify-center flex-shrink-0"><Shield className="w-5 h-5 text-primary" /></div><div><p className="font-bold text-base text-primary">Sessões Semanais ao Vivo</p><p className="text-sm text-muted-foreground">Onboarding em grupo e sessões de tira-dúvidas semanais para garantir que sua escola tire o máximo do Orbit.</p></div></div></div></section>

      <section className="px-5 py-8 lg:py-14 lg:px-16"><div className="max-w-6xl mx-auto"><div className="text-center mb-8 lg:mb-12"><h2 className="text-xl lg:text-3xl font-extrabold text-foreground uppercase tracking-tight mb-2">O que empresários dizem{" "}<span className="text-primary">sobre o Orbit</span></h2></div><div className="grid grid-cols-1 md:grid-cols-2 gap-4 lg:gap-6 max-w-3xl mx-auto">{testimonials.map((t, i) => (<div key={i} className="relative bg-card rounded-xl p-5 lg:p-6 border border-border hover:border-primary/20 transition-all duration-300"><div className="absolute -top-2 right-4"><div className="w-8 h-8 rounded-full bg-primary/20 flex items-center justify-center"><Quote className="w-4 h-4 text-primary" /></div></div><p className="text-sm text-muted-foreground leading-relaxed mb-4">"{t.text}"</p><div className="flex items-center gap-2"><div className="w-9 h-9 rounded-full bg-primary flex items-center justify-center"><span className="text-primary-foreground font-bold text-sm">{t.initial}</span></div><div><p className="font-semibold text-sm text-foreground">{t.name}</p><p className="text-xs text-muted-foreground">{t.company}</p></div></div></div>))}</div></div></section>

      <section className="px-5 py-8 lg:py-14 lg:px-16 bg-secondary/50"><div className="max-w-2xl mx-auto"><h2 className="text-xl lg:text-2xl font-extrabold text-foreground uppercase tracking-tight text-center mb-6">Perguntas <span className="text-primary">Frequentes</span></h2><FAQSection /></div></section>

      <section className="px-5 py-8 lg:py-16 lg:px-16"><div className="max-w-md mx-auto text-center"><h2 className="text-xl lg:text-3xl font-extrabold text-foreground uppercase tracking-tight mb-3">Pronto para tirar sua escola do{" "}<span className="text-primary">caos operacional?</span></h2><p className="text-sm text-muted-foreground mb-6">Agende uma demonstração gratuita de 30 minutos. Descubra como dezenas de agentes de IA podem organizar sua {NICHE_ENTITY_FULL} — sem contratar mais ninguém.</p><button onClick={() => setShowModal(true)} className="w-full max-w-sm mx-auto py-5 px-8 rounded-xl font-extrabold text-lg tracking-wide uppercase transition-all duration-200 flex items-center justify-center gap-2 bg-primary text-primary-foreground hover:brightness-110 active:scale-[0.98] shadow-lg shadow-primary/30">{variant.cta}</button><p className="text-center text-xs text-muted-foreground mt-2.5">⏱️ 2 min • 100% gratuito • Sem compromisso</p></div></section>

      <footer className="bg-card border-t border-border text-foreground py-10 sm:py-12 md:py-16"><div className="container mx-auto px-4 sm:px-6"><div className="flex flex-col md:flex-row md:items-start md:justify-between gap-6 sm:gap-8 lg:gap-12 mb-8 sm:mb-10 md:mb-12"><div className="max-w-sm"><div className="flex items-center gap-2 sm:gap-3 mb-3 sm:mb-4"><img src={orbitLogoColor} alt="Orbit" className="h-8 sm:h-10 w-auto" /><span className="font-bold text-lg sm:text-xl">Orbit</span></div><p className="text-muted-foreground text-sm sm:text-base mb-4 sm:mb-6">Time de agentes de IA para gestão empresarial. Dezenas de especialistas trabalhando 24/7 para organizar sua {NICHE_ENTITY_FULL}.</p><div className="flex gap-2 sm:gap-3"><a href="https://www.instagram.com/orbitgestao" target="_blank" rel="noopener noreferrer" className="w-9 h-9 sm:w-10 sm:h-10 rounded-full bg-secondary flex items-center justify-center hover:bg-secondary/80 transition-colors"><Instagram className="w-4 h-4 sm:w-5 sm:h-5" /></a></div></div><div><h4 className="font-bold text-base sm:text-lg mb-3 sm:mb-4">Contato</h4><ul className="space-y-3 sm:space-y-4"><li className="flex items-center gap-2 sm:gap-3"><Phone className="w-4 h-4 sm:w-5 sm:h-5 text-primary flex-shrink-0" /><a href="https://wa.me/5548991206282" target="_blank" rel="noopener noreferrer" className="text-muted-foreground hover:text-foreground transition-colors text-sm sm:text-base">(48) 99120-6282</a></li><li className="flex items-start gap-2 sm:gap-3"><MapPin className="w-4 h-4 sm:w-5 sm:h-5 text-primary flex-shrink-0 mt-0.5" /><span className="text-muted-foreground text-sm sm:text-base">Square SC - Rod. José Carlos Daux, 5500<br />Saco Grande, Florianópolis - SC, 88032-005</span></li></ul></div></div><div className="pt-6 sm:pt-8 border-t border-border flex flex-col sm:flex-row items-center justify-between gap-3 sm:gap-4"><p className="text-xs sm:text-sm text-muted-foreground/50 text-center sm:text-left">© {new Date().getFullYear()} Orbit. Todos os direitos reservados.</p><div className="flex gap-4 sm:gap-6"><a href="/privacidade" className="text-xs sm:text-sm text-muted-foreground/50 hover:text-muted-foreground transition-colors">Política de Privacidade</a><a href="/termos" className="text-xs sm:text-sm text-muted-foreground/50 hover:text-muted-foreground transition-colors">Termos de Uso</a></div></div></div></footer>
    </div>
  );
};

export default Educacao;
