# Supabase — chaves "publishable / anon" (seguras pro frontend, protegidas por RLS no DB).
# Encontre em: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL="https://SEU_PROJETO.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="eyJhbGciOi..."
VITE_SUPABASE_PROJECT_ID="SEU_PROJETO"

# Duplicatas usadas por scripts/tooling fora do Vite (não expostas no bundle)
SUPABASE_URL="https://SEU_PROJETO.supabase.co"
SUPABASE_PUBLISHABLE_KEY="eyJhbGciOi..."

# Observação: secrets de server-side (PIPEDRIVE_API_TOKEN, MANYCHAT_API_TOKEN,
# RESEND_API_KEY, LOVABLE_API_KEY, ELEVENLABS_API_KEY, SUPABASE_SERVICE_ROLE_KEY)
# não vão aqui — são configurados no painel do Supabase:
# Edge Functions → Manage secrets
